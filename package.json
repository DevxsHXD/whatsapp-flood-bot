{
  "name": "whatsappbot",
  "version": "1.0.0",
  "description": "Whatsapp bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc --build && node index.js"
  },
  "author": "Usitha Indeewara",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "@types/qrcode-terminal": "^0.12.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.21.0"
  }
}
